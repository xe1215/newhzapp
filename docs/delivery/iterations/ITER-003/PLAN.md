# ITER-003 Plan

1. 主协调对照 UAPD 1.3 复核现有角色、文件、模式、闸门与交接要求。
2. 用正式工作流内容替换 Skill 占位模板，固定本项目事实源路径和授权边界。
3. 增加调用元数据、模式示例与任务包/Handoff 模板。
4. 在 README 增加用户调用入口。
5. 执行结构与解析验证，同步 current、tasks、decisions、handoff、results 和 retro。

文件边界仅限 `.agents/skills/product-delivery-workflow/**`、`README.md` 和 `docs/delivery/**`。
