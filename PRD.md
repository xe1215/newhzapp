# NewHz 智能口红试色产品需求文档

> 文档版本：2.0
>
> 现状基线：2026-09-12 当前仓库代码
>
> 文档性质：现行产品规格，不代表生产环境已完成验收

## 1. 产品定义

NewHz 是一个微信小程序口红试色与个性化推荐产品。用户上传自拍并选择肤色、脸型、预算、场景和风格后，系统从运营维护的口红库中选出三支不同品牌、不同颜色的推荐，生成对应试色图。免费阶段展示带水印预览；用户解锁后查看无水印试色和完整推荐内容，并可回看或分享单支结果。

仓库同时包含一个只供单一开发者使用的桌面端 Web 运营后台，用于查看业务运行情况、维护口红库与推荐规则、排查记录和标记异常。它不是多管理员后台，也不代替微信公众平台、微信支付商户平台或 CloudBase 控制台。

## 2. 目标用户与价值

### 小程序用户

- 不确定适合什么口红颜色，希望先看到自己的上脸效果。
- 需要按肤色、脸型、预算和使用场景获得更具体的建议。
- 愿意为低价、个性化且可回看的完整报告付费。

### 开发者

- 需要维护口红和组合推荐规则。
- 需要观察访问、测试、生成、支付、报告和分享链路。
- 需要定位生成失败、异常订单和报告交付问题。

### 核心价值

将“看通用试色图”变成“看自己的试色结果”，并用明确、低门槛的解锁路径验证个性化美妆建议的付费价值。

## 3. 当前产品状态

状态含义：

- 已实现：仓库中存在完整调用链和主要状态处理。
- 受限实现：主流程存在，但依赖 mock、调用方声明或尚未完成生产级集成。
- 待验证：代码存在，但缺少生产环境、真实账户或真实数据证据。

| 能力 | 状态 | 当前说明 |
|---|---|---|
| 微信静默登录 | 已实现 | 通过微信上下文获取 openid，创建或更新 `users` |
| 自拍拍摄/相册上传 | 已实现 | 上传临时文件后由 `test.uploadSelfie` 移入用户私有路径 |
| 内容安全与质量检查 | 受限实现 | 云函数能按检查结果拒绝并删除文件，但当前检查值由调用方传入；小程序当前未接真实审核服务 |
| 偏好选择 | 已实现 | 当前主入口在上传页，字段为肤色、脸型、预算、场景、风格；旧 preferences 页会重定向回上传页 |
| 规则推荐 | 已实现 | 支持配置组合规则优先；否则按预算过滤、肤色/场景/风格/人工权重打分，并约束品牌和颜色去重 |
| 三图试色生成 | 已实现，待生产验证 | 支持 mock 与即梦异步适配器、任务续查、重试、超时诊断和 provider run 记录 |
| 水印预览 | 已实现，待视觉验证 | 正式图下载后程序添加 `PREVIEW` 水印并分别存储 |
| 免费换组 | 已实现 | 最多成功换三次；排除已用口红；失败或异步未完成不消耗次数 |
| 预览与锁定报告 | 已实现 | 预览只返回水印图和次数；锁定报告不返回付费内容 |
| 5.99 元订单与解锁 | 受限实现 | 订单绑定当前 active report，确认逻辑幂等；当前使用 mock prepay/transaction id，未接真实 `wx.requestPayment` 和可信支付回调 |
| 完整报告 | 已实现 | 解锁后返回无水印图、推荐快照、共享建议和原图（未被用户删除时） |
| 我的报告与历史 | 已实现 | 仅列出本人已解锁且未隐藏报告，支持三支推荐卡片和历史入口 |
| 分享卡片与落地页 | 已实现，待生产验证 | 只允许分享已解锁报告中的单支推荐，记录访问与独立访客 |
| 删除自拍 | 已实现 | 用户主动删除原图不删除生成报告 |
| 自动清理 | 已实现，待部署验证 | 每小时任务清理到期未付自拍和报告资产；已付报告关联原图被保留 |
| 退款申请 | 受限实现 | 只对已支付但不可查看报告的订单记录退款申请；实际退款仍在微信支付商户平台 |
| 开发者后台 | 已实现，待环境验收 | 六个主模块、登录、筛选、详情、CSV 和有限写操作已存在 |

## 4. 当前用户流程

### 4.1 新建试色

1. 用户进入首页，小程序静默登录。
2. 用户从相机或相册选择一张自拍。
3. 客户端上传临时文件，云函数校验并将合格文件移动到 `selfies/{openid}/{testId}/original.jpg`。
4. 用户在上传页选择肤色、脸型、预算、场景和风格。
5. 系统生成三支推荐快照和一个 active report。
6. 生成页调用图像服务；异步任务未完成时保留生成状态并继续查询。
7. 三张正式图完成后生成三张水印图，用户进入预览页。

### 4.2 预览与换组

1. 预览页展示 A/B/C 三张带水印卡片，不暴露品牌、色号和报告文案。
2. 用户可滑动切换；点当前卡片进入对应的锁定报告预览。
3. 用户可选择“换一组”。系统排除当前测试历史报告中已出现的口红。
4. 只有新一组完整生成并激活后才增加成功换组次数；最多三次。
5. 当前 report 变为 replaced，新 report 成为 active；异步生成中的临时 report 不可支付。

### 4.3 解锁与报告

1. 用户从预览或锁定报告发起解锁。
2. 系统读取 `try_on_tests.activeReportId` 创建固定绑定 report 的 5.99 元订单。
3. 当前代码通过模拟确认完成支付状态更新；生产版本必须替换为微信支付下单和服务端可信回调。
4. 只有订单绑定 report 存在无水印图时才可查看，否则订单进入退款待处理状态。
5. 报告支持三支推荐切换、原图/试色对比、建议内容、分享和主动删除原图。

### 4.4 回看、分享与退款

- “报告”展示最新已解锁报告的三支推荐；“历史报告”按报告维度导航。
- 用户可以隐藏报告；隐藏不删除订单历史。
- 分享只生成一支推荐的卡片和公开落地数据，不公开整份报告或原自拍。
- 退款页面只提交异常退款申请；实际资金退款由开发者在微信支付商户平台处理。

## 5. 核心业务规则

### 推荐

- 必须从 `status=active` 的口红中选择。
- 若命中 `recommendation_rules` 的肤色 + 脸型 + 预算组合，并配置三支有效口红，则优先采用配置组合。
- 否则先按预算过滤，再按肤色 +100、场景 +20、风格 +10、`manualBoost` 加权排序。
- 同组推荐必须有三个不同品牌和三个不同 `colorHex`；不足三支返回 `RECOMMENDATION_NOT_ENOUGH`。
- 报告保存推荐快照，后续口红库修改不应改变历史报告核心内容。

### 生成

- 每个 report 固定三支推荐和三组图片位置。
- 即梦适配器逐支提交/续查异步任务，保存任务诊断；超时、任务过期或供应商错误需要返回可识别错误码。
- 无水印图写入 `paidImages`，带水印图写入 `previewImages`，两者不得覆盖。
- 试色要求尽量只改变嘴唇颜色与质地，保留身份、构图、肤色、光照、牙齿和面部结构。

### 支付与解锁

- 报告价格固定为 599 分，币种 CNY。
- 创建订单时只绑定当前 active report；过期、删除或非本人 report 不得下单。
- 支付确认必须幂等，重复确认不能重复解锁或重复产生业务副作用。
- 完整报告只对本人且已解锁 report 返回付费资产。
- 真实支付成功必须由可信服务端回调确认，不能依赖客户端传入 transaction id；这是生产上线前的阻塞项。

### 生命周期与隐私

- 自拍与业务记录按 openid 隔离，用户侧核心 API 必须校验资源归属。
- 未支付测试默认 24 小时到期；定时任务删除原图与未付 report 图片并清空敏感快照。
- 已解锁报告或已支付订单关联的测试原图不被自动清理，以支持原图/结果对比；支付确认页要求用户确认保留说明。
- 用户可随时主动删除原自拍，报告和订单保留。
- 分享落地页只返回单支推荐与分享统计，不返回原自拍或整份付费报告。

## 6. 开发者后台

### 已实现模块

- 运营仪表盘：今天、昨天、近 7 天、近 30 天；漏斗、转化、趋势、最近订单、生成失败和异常订单。
- 口红库维护：搜索、筛选、新增、编辑、上下架、整批校验 CSV 导入和 CSV 导出。
- 推荐规则：按肤色、脸型、预算配置三支口红及共享建议文案。
- 测试记录：筛选列表和完整详情，只读。
- 报告记录：筛选、详情、图片链接检查和异常标记。
- 订单与退款：筛选与详情；实际退款仍在微信支付平台。
- 生成与事件日志：provider runs / events 列表详情及事件 CSV 导出。

### 权限边界

- 默认要求开发者用户名、密码哈希和服务端 session；session 存入 `admin_sessions` 并支持注销。
- `ADMIN_AUTH_DISABLED=true` 仅允许临时开发环境使用，生产必须关闭。
- 列表默认脱敏 openid，详情和主动复制/导出可使用完整值。
- 口红、推荐规则和报告标记等写操作记录 `admin_actions`。
- 后台浏览器需要 CloudBase 可发布访问凭据，但不得嵌入管理密钥或云函数敏感环境变量。

## 7. 范围

### 当前版本范围

- 自拍试色、偏好推荐、三图生成、水印预览、三次换组。
- 固定报告订单、解锁报告、报告回看、分享和生命周期管理。
- 单开发者运营后台。

### 明确不做

- 商城、购买链接、优惠券、会员、收藏、评论或社区。
- 多管理员账号、RBAC、组织或审批流。
- 自动实际退款、复杂客服工单。
- 复杂 BI、渠道归因、用户画像和多模型实验平台。
- 其他彩妆品类或全脸妆容。

## 8. 产品验收与发布阻塞

### 功能验收

- 用户可以从自拍上传完成到三张预览，并正确处理加载、失败、续查和重试。
- 推荐始终满足三支、品牌去重、颜色去重和预算过滤。
- 成功换组最多三次，失败不扣次数，旧报告不能被新订单误解锁。
- 锁定态不泄露付费推荐；解锁态只对本人返回正式图和完整内容。
- 删除原图、隐藏报告、分享单支和异常退款申请符合上述边界。
- 后台只开放约定写操作，所有敏感模块要求有效开发者会话。

### 生产上线阻塞项

- 接入真实、服务端可信的内容安全与图像质量检查。
- 接入微信支付统一下单、`wx.requestPayment` 和验签/幂等支付回调，移除 mock 确认链路。
- 在目标 CloudBase 环境验证即梦凭据、异步任务、图片水印、存储权限和定时清理。
- 分类并处理当前 18 个失败测试文件，使回归结论可信。
- 建立真实生产指标基线与告警，不以测试数据替代。

## 9. 成功指标

指标口径与待采集基线见 `docs/delivery/metrics.md`。核心指标为上传成功率、生成成功率、预览到支付转化率、支付交付成功率、报告查看率和分享访问率；退款率、生成 P95 和严重缺陷数作为护栏。
